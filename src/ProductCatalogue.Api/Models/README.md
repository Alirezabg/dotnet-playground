# Models

Place your request/response DTOs here.

Consider using C# `record` types for immutability.

**Hint from your instructor:** What is the difference between a domain entity and a DTO?
Why should your API not expose domain entities directly?
