# Endpoints

Place your endpoint extension classes here.

One file per resource group — e.g. `ProductEndpoints.cs`, `CategoryEndpoints.cs`.

**Hint from your instructor:** Before you create a file, ask yourself:
- What HTTP verbs will this resource expose?
- What should each route return on success and on error?
- How does this endpoint connect to a service in the layer below?
