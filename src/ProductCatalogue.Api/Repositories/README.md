# Repositories

Place your data access code here (in-memory or otherwise).

**Hint from your instructor:** What pattern are you implementing? 
What is the contract (interface) a repository should expose?
Why do we put repositories behind interfaces in interview scenarios?
